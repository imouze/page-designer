import BaseLayer from './base';

class ImageLayer extends BaseLayer{
    constructor(options){
        super(options);
    }

    _render(){
        super._render();
    }
}

module.exports = ImageLayer;